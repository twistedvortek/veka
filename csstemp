/* Basic Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Roboto', sans-serif;
    background-color: #23272A; /* Discord's dark grey */
    color: white;
    line-height: 1.6;
}

a {
    color: orange;
    text-decoration: none;
}

/* Header Styles */
header {
    display: flex;
    justify-content: space-between;
    padding: 20px;
    background-color: #1E1E1E; /* Slightly darker shade for header */
    color: white;
}

.logo {
    font-weight: 700;
    font-size: 1.5rem;
}

.mode-switch {
    background: none;
    border: 1px solid orange;
    color: orange;
    padding: 5px 10px;
    cursor: pointer;
}

/* Loading Screen */
#loading-screen {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-color: #23272A;
    color: white;
    font-size: 2rem;
}

/* Main Content */
#main-content {
    display: block; /* Change to block to make content visible after loading */
}

/* Slider */
.slider {
    overflow: hidden;
}

.slide {
    display: none; /* Hide slides by default, display with JavaScript */
    text-align: center;
}

/* First slide visible by default */
.slide.active {
    display: block;
}

/* Category Sections & Diagonal Cuts */
.category-section {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin: 20px 0;
}

.diagonal-cut {
    clip-path: polygon(0 0, 100% 0, 100% 85%, 0 100%);
    width: 45%; /* Adjust based on design preference */
    transition: transform 0.3s ease;
}

/* Hover effect for diagonal images */
.diagonal-cut:hover {
    transform: scale(1.05);
}

/* Text Content within Sections */
.text-content {
    width: 50%;
}

/* Social Icons */
.social-icons a {
    color: white;
    margin: 0 10px;
}

.social-icons i {
    font-size: 24px;
}

/* News Section */
.main-news {
    width: 60%;
    margin-right: 20px;
}

.other-news {
    width: 40%;
    display: flex;
    flex-direction: column;
    justify-content: space-around;
}

.article {
    background: #333;
    padding: 10px;
    margin-bottom: 10px;
}

/* Footer */
footer {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    padding: 20px;
    background-color: #2C2F33;
}

.footer-column {
    width: 18%; /* Adjust width for a 5-column layout */
    margin: 10px;
    text-align: left;
}

.footer-column a, .footer-column p, .footer-column address {
    margin-bottom: 10px;
    display: block;
}

/* Responsive Design */
@media (max-width: 768px) {
    header, .text-content, .footer-column {
        text-align: center;
    }

    .category-section, .footer-column {
        flex-direction: column;
        width: 100%;
    }

    .diagonal-cut, .text-content {
        width: 100%;
    }
}
    